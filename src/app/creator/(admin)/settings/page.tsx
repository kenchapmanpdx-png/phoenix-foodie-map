import CreatorSettings from '@/components/creator/CreatorSettings'

export const metadata = {
  title: 'Settings - Creator Portal',
}

export default function SettingsPage() {
  return <CreatorSettings />
}
